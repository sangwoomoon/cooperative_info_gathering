function obj = Declare ( obj )
%DEFAULT generates null space of measurement class, so more specified
%option for measurement class should be defined by users

fprintf('Agent Measurement is not assigned : should be modelized by users!\n')

end